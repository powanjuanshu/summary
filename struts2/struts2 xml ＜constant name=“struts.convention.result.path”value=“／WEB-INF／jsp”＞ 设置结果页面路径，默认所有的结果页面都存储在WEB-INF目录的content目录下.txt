设置结果页面路径，默认所有的结果页面都存储在WEB-INF/content下，你可以通过设置这个属性来改变到其他路径
---------------
<struts>
	<constant name="struts.convention.result.path" value="/WEB-INF/jsp"/><!--设置结果页面路径，默认所有的结果页面都存储在WEB-INF/content下，你可以通过设置这个属性来改变到其他路径-->
</struts>