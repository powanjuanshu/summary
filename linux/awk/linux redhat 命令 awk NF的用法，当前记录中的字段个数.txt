文件b如下：
1:2:3
4:5:6
7:8
---------------------
输入命令：
awk -F ':' '{print NF}' b
---------------------
输出结果：
3
3
2
---------------------
NF：当前记录中的字段个数
awk -F ':' '{print NF}' b的输出为
3
3
2
表明b的每一行用分隔符":"分割后是3个字段，3字段，2字段
可以用NF来控制输出符合要求的字段数的行，这样可以处理掉一些异常的行
awk -F ':' '{if (NF == 3)print}' b


