如果没有commit，硬盘里的数据是没有修改的，如果commit了，也有可能没有修改，但是日志会记录下来。因为oracle有个批量写入的机制，在写入操作比较多的时候，commit
并不能把修改的内容立即写入到硬盘中去，而是放入缓存区中，日志文件中记录，等待代写的内容积累到一定数量再批量写入到硬盘中。

oracle中dml语句在执行后都必须显示commit，否则，在重启后系统（关系数据库再重新启动）会自动回滚rollback，所以必须在关之前用commit进行提交。