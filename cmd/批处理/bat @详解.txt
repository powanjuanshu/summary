@echo off 关闭回显

DOS在运行批处理时，会依次执行批处理中的每条命令，并且会在显示器上显示，如果你不想让它们显示，可以加一个"echo off"
当然，"echo off"也是命令，它本身也会显示，如果连这条也不显示，就在前面加个"@"

@pause是什么意思
DOS内部命令：暂时停止批处理文件的执行前面加了@，就是连本身也不显示
-----------------------------------------
@echo off
ipconfig /all
@pause

