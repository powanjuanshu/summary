如果将单个基本类型数组的元素传递给方法,并在方法中对其进行修改,则在被调用方法结束执行时,该元素中存储的并不是修改后的值,因为这种元素是按值传递。
如果传递的是数组的引用,则对数组元素的后续修改可以在原始数组中反映出来
(因为数组本身就是个对象，int[] a = new int[2];这里面的int是数组元素的类型，而数组元素的修改是操作对象)

对于单个非基本类型数组的元素在方法中修改,则在被调用方法结束执行时,该元素中存储的是修改后的值,因为这种元素是按引用传递的,对象的改动将在源数组的数组元素中反映出来。
